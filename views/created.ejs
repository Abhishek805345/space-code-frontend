<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Room Created Successfully</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #0f172a, #1e293b);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
    }

    .success-card {
      background: #020617;
      padding: 40px 35px;
      border-radius: 14px;
      width: 100%;
      max-width: 420px;
      text-align: center;
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    }

    .check-icon {
      width: 70px;
      height: 70px;
      margin: 0 auto 20px;
      border-radius: 50%;
      background: #22c55e;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 36px;
      font-weight: bold;
      color: white;
    }

    .success-card h1 {
      font-size: 26px;
      margin-bottom: 10px;
    }

    .success-card p {
      font-size: 15px;
      color: #cbd5f5;
      margin-bottom: 30px;
    }

    .room-id {
      background: #020617;
      border: 1px dashed #334155;
      padding: 12px;
      border-radius: 8px;
      font-size: 14px;
      margin-bottom: 30px;
      color: #e5e7eb;
    }

    .btn-group {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
    }

    .btn {
      flex: 1;
      padding: 12px;
      border-radius: 8px;
      border: none;
      font-size: 15px;
      cursor: pointer;
      transition: 0.2s ease;
      text-decoration: none;
      text-align: center;
    }

    .btn-home {
      background: #334155;
      color: white;
    }

    .btn-home:hover {
      background: #475569;
    }

    .btn-room {
      background: #2563eb;
      color: white;
    }

    .btn-room:hover {
      background: #1d4ed8;
    }
  </style>
</head>
<body>

  <div class="success-card">
    <div class="check-icon">✓</div>

    <h1>Room Created!</h1>
    <p>Your project room has been created successfully.</p>

    <!-- Optional room ID display -->
    <div class="room-id">
      Room ID: <strong> <%= roomid %></strong>
    </div>

    <div class="btn-group">
      <a href="/after-home/<%=userid%>">Home</a>
      <form action="/room/<%=roomid%>" method="POST">
        <input type="hidden" value="<%=userid%>" name="userid">
        <button class="btn btn-room" type="submit">Enter the room</button>
      </form>
    </div>
  </div>

</body>
</html>
