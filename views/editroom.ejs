<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>My Rooms | Space Code</title>

<style>
/* =======================
   RESET & BASE
======================= */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: Inter, Arial, sans-serif;
}

body {
  background: #f8fafc;
  color: #0f172a;
}

/* =======================
   NAVBAR (SAME AS DASHBOARD)
======================= */
nav {
  height: 60px;
  background: #0f172a;
  color: #e5e7eb;
  padding: 0 32px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: relative;
  z-index: 10;
  box-shadow: 0 4px 16px rgba(15,23,42,0.25);
}

nav .logo {
  font-size: 20px;
  font-weight: 700;
}

nav ul {
  list-style: none;
  display: flex;
  align-items: center;
  gap: 20px;
}

nav ul li a {
  color: #e5e7eb;
  text-decoration: none;
  font-size: 14px;
  padding: 6px 10px;
  border-radius: 6px;
}

nav ul li a:hover {
  background: rgba(255,255,255,0.08);
}

/* Hamburger */
#hambutton {
  background: transparent;
  border: none;
  cursor: pointer;
}

/* =======================
   HAMBURGER MENU
======================= */
.ham {
  position: absolute;
  top: 64px;
  right: 24px;
  width: 220px;
  background: #020617;
  border-radius: 14px;
  padding: 12px;
  display: none;
  box-shadow: 0 20px 40px rgba(0,0,0,0.45);
}

.ham.active {
  display: block;
}

.ham button {
  width: 100%;
  background: transparent;
  border: none;
  color: #e5e7eb;
  padding: 10px;
  text-align: left;
  cursor: pointer;
  border-radius: 10px;
}

.ham button:hover {
  background: #1e293b;
}

/* =======================
   PAGE HEADER
======================= */
.page-header {
  max-width: 1200px;
  margin: 40px auto 20px;
  padding: 0 24px;
}

.page-header h1 {
  font-size: 30px;
  font-weight: 700;
}

.page-header p {
  color: #64748b;
  margin-top: 6px;
}

/* =======================
   ROOMS GRID
======================= */
.rooms-container {
  max-width: 1200px;
  margin: 0 auto 80px;
  padding: 0 24px;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 24px;
}

/* =======================
   ROOM CARD
======================= */
.room-card {
  background: #ffffff;
  border-radius: 16px;
  padding: 22px;
  border: 1px solid #e5e7eb;
  box-shadow: 0 6px 18px rgba(15,23,42,0.06);
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  transition: transform 0.25s ease;
}
/* =======================
   FORCE EQUAL ACTION WIDTHS
   (NO HTML CHANGES)
======================= */
.card-actions > * {
  flex: 1;
  display: flex;
}

.card-actions > * > .btn {
  width: 100%;
}


.room-card:hover {
  transform: translateY(-4px);
}

.room-card h3 {
  font-size: 18px;
  font-weight: 700;
}

.room-card p {
  font-size: 14px;
  color: #64748b;
  margin: 10px 0 18px;
}

/* =======================
   CARD ACTION BUTTONS
======================= */
.card-actions {
  display: flex;
  gap: 10px;
}

.btn {
  flex: 1;
  padding: 10px;
  border-radius: 10px;
  border: none;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
}

/* Edit */
.btn-edit {
  background: linear-gradient(135deg, #4f46e5, #4338ca);
  color: #ffffff;
}

/* Delete */
.btn-delete {
  background: #ffffff;
  color: #991b1b;
  border: 1px solid #fecaca;
}

.btn-delete:hover {
  background: #fef2f2;
}

/* =======================
   FOOTER
======================= */
footer {
  position:fixed;
  bottom: 0px;
  width:100vw;
  background: #0f172a;
  color: #94a3b8;
  padding: 36px;
  text-align: center;
  margin-top: 80px;
  font-size: 14px;
}
/* =======================
   DELETE CONFIRMATION MODAL
======================= */

/* Overlay (background blur / darken) */
.delcard {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.95);
  width: 100%;
  max-width: 420px;
  background: #ffffff;
  border-radius: 16px;
  padding: 26px 28px;
  border: 1px solid #e5e7eb;
  box-shadow: 0 20px 45px rgba(15, 23, 42, 0.25);
  text-align: center;
  z-index: 1000;
  display: none; /* toggle via JS */
  animation: fadeIn 0.25s ease forwards;
}

/* Text */
.delcard p {
  font-size: 15px;
  color: #334155;
  font-weight: 600;
  line-height: 1.5;
  margin-bottom: 22px;
}

/* Buttons container */
.delcard a,
.delcard button {
  display: inline-block;
}

/* Delete button */
.delcard form button {
  padding: 11px 26px;
  border-radius: 10px;
  border: none;
  background: linear-gradient(135deg, #ef4444, #dc2626);
  color: #ffffff;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  margin-right: 10px;
  box-shadow: 0 6px 16px rgba(239, 68, 68, 0.25);
  transition: all 0.25s ease;
}

.delcard a button:hover {
  transform: translateY(-1px);
  box-shadow: 0 10px 22px rgba(239, 68, 68, 0.35);
}

/* Cancel button */
.delcard button:last-child {
    margin:4px;
  box-shadow: 1px 1px 3px black;
  width:fit-content;
  padding: 10px 20px;
  border-radius: 10px;
  border: 1px solid #e5e7eb;
  background: #ffffff;
  color: #475569;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease;
}

.delcard button:last-child:hover {
  background: #f1f5f9;
}

/* =======================
   FADE ANIMATION
======================= */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translate(-50%, -48%) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1);
  }
}

</style>
</head>

<body>

<!-- NAVBAR -->
<nav>
  <div class="logo">Space Code</div>
  <ul>
    <li><a href="/post-home/<%=userid%>">Home</a></li>
    <li><a href="/get-room/<%= userid %>">Code Rooms</a></li>
    <li><a href="/get-interview-room/<%= userid %>">Interview Rooms</a></li>
    <li><a href="/logout/<%=userid%>">Logout</a></li>
    <li>
      <button id="hambutton" onclick="expand()">
        <img src="/ham.png" width="22">
      </button>
    </li>
  </ul>
</nav>

<div class="ham">
<a href="/account/setting/<%=userid%>"><button>settings</button></a> 
</div>

<!-- HEADER -->
<div class="page-header">
  <h1>My Rooms</h1>
  <p>You can change config. and delete your project rooms</p>
</div>

<!-- ROOMS -->
<div class="rooms-container">

  <!-- ROOM CARD -->
 <% roomdata.forEach(data=>{%>
       <div class="room-card">
    <div>
      <h3><%=data.projectName%></h3>
      <p><%=data.description%></p>
    </div>

    <div class="card-actions">
      <a href="/edit/room/<%=data._id%>"><button class="btn btn-edit">Edit</button></a>
      <button class="btn btn-delete" onclick="delexpan()">Delete</button>
      <!--  -->
    </div>
  </div>
        <div class="delcard">
    <p>are you sure want to delete the room all the data will be lost that you saved in it</p>
    <form action="/delete/room/<%=data._id%>" method="POST">
      <input type="hidden" value="<%=userid%>" name="userid">
      <button>Delete</button>
      
    </form>
    <button class="canbutton" onclick="delhide()">Cancel</button>
    
  </div>

<%})%>

</div>

<footer>
  © 2026 Space Code. All rights reserved.
</footer>

<script>
function expand() {
  document.querySelector('.ham').classList.toggle('active');
}
function delexpan() {
  document.querySelector('.delcard').style.display = 'block';
}

function delhide() {
  document.querySelector('.delcard').style.display = 'none';
}

</script>

</body>
</html>
