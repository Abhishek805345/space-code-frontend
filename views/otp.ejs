<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Verify OTP</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", sans-serif;
    }

    body {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #11998e, #38ef7d);
    }

    .container {
      background: #ffffff;
      padding: 32px 36px;
      width: 100%;
      max-width: 360px;
      border-radius: 12px;
      box-shadow: 0 12px 35px rgba(0, 0, 0, 0.18);
      text-align: center;
    }

    h2 {
      margin-bottom: 8px;
    }

    .subtitle {
      font-size: 14px;
      color: #666;
      margin-bottom: 22px;
      line-height: 1.4;
    }

    .otp-box {
      display: flex;
      justify-content: space-between;
      margin-bottom: 20px;
    }

    #otp-box {
      width: 100%;
      height: 55px;
      text-align: center;
      font-size: 20px;
      border-radius: 8px;
      border: 1px solid #ccc;
      outline: none;
    }

    .otp-box input:focus {
      border-color: #11998e;
    }

    button {
      width: 100%;
      padding: 11px;
      background: #11998e;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background: #0f8a7f;
    }

    .links {
      margin-top: 18px;
      font-size: 14px;
    }

    .links a {
      color: #11998e;
      text-decoration: none;
      font-weight: 500;
    }
  </style>
</head>

<body>

  <div class="container">
    <h2>OTP Verification</h2>
    <p class="subtitle">
      Enter the 4-digit OTP sent to your email
    </p>

    <form action="/verify-otp/<%=id%>" method="POST">
      <div class="otp-box">
        <input id="otp-box" type="text" maxlength="4" name="otp" required />
      </div>

      <button type="submit">Verify OTP</button>
    </form>

    <div class="links">
      <a href="/forgot-password">Resend OTP</a>
    </div>
  </div>

</body>
</html>
