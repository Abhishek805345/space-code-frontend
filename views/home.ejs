<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #f4f6f8;
        }

        /* Navbar */
        nav {
            background-color: #1f2933;
            color: #fff;
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 10;
        }

        nav .logo {
            font-size: 22px;
            font-weight: bold;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 16px;
            padding: 6px 10px;
            border-radius: 4px;
        }

        nav ul li a:hover {
            background-color: #3a4a5a;
        }

        /* Hero Section */
        .hero {
            position: relative;
            height: 70vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            background-color: black;
        }

        .hero-video {
            position: absolute;
            inset: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.25;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            text-align: center;
            color: white;
        }

        .hero-content h1 {
            font-size: 48px;
            margin-bottom: 10px;
        }

        .hero-content p {
            font-size: 20px;
        }

        /* Dashboard Content */
        .container {
            padding: 40px;
            text-align: center;
        }

        .container h1 {
            color: #1f2933;
            margin-bottom: 10px;
        }

        .container p {
            color: #555;
            font-size: 18px;
        }

        /* Button */
        .create-project-btn {
            display: inline-block;
            margin-top: 30px;
            padding: 12px 30px;
            background-color: #2563eb;
            color: white;
            text-decoration: none;
            font-size: 16px;
            border-radius: 6px;
        }

        .create-project-btn:hover {
            background-color: #1d4ed8;
        }

        /* ===== ADDED STYLES (no existing change) ===== */

        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-top: 50px;
        }

        .stat-card {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.08);
        }

        .stat-card h3 {
            font-size: 32px;
            color: #2563eb;
        }

        .features {
            margin-top: 70px;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 25px;
            margin-top: 25px;
        }

        .feature-card {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.08);
            text-align: left;
        }

        .feature-card h3 {
            color: #2563eb;
            margin-bottom: 10px;
        }

        .recent {
            margin-top: 70px;
        }

        .recent-box {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.08);
            text-align: left;
            max-width: 800px;
            margin: auto;
        }

        footer {
            background: #1f2933;
            color: #cbd5e1;
            padding: 50px 30px;
            margin-top: 80px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 30px;
        }

        footer h3 {
            color: white;
            margin-bottom: 12px;
        }

        footer a {
            color: #cbd5e1;
            text-decoration: none;
            display: block;
            margin-bottom: 8px;
        }

        footer a:hover {
            color: white;
        }

        .footer-bottom {
            text-align: center;
            margin-top: 30px;
            font-size: 14px;
            color: #94a3b8;
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav>
        <div class="logo">Space Code</div>
        <ul>
            <li><a href="/post-home/<%=data.data._id%>">Home</a></li>        <!--  bcz i passed the complete in result as {data} in backend-->
            <li><a href="/get-room/<%=data.data._id%>">Project Rooms</a></li>   
            <li><a href="/logout/<%= data.data._id %>">Logout</a></li>
        </ul>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <video autoplay muted loop playsinline class="hero-video">
            <source src="/hero.mp4" type="video/mp4">
        </video>

        <div class="hero-content">
            <h1>Welcome, <%= data.data.username %> 👋</h1>
            <p>You have successfully logged in</p>
        </div>
    </section>

    <!-- Dashboard Section -->
    <div class="container">
        <h1>Dashboard</h1>
        <p>Manage your account, settings, and activities from here.</p>

        <a href="/create-project/<%= data._id %>" class="create-project-btn">
            Create Project
        </a>

        <!-- Stats -->
        <div class="stats">
            <div class="stat-card">
                <h3>5</h3>
                <p>Projects Created</p>
            </div>
            <div class="stat-card">
                <h3><%=data.data.mycurrentroom%></h3>
                <p>Active Rooms</p>
            </div>
            <div class="stat-card">
                <h3><%=data.data.mycurrentroom%></h3>
                <p>Collaborations</p>
            </div>
            <div class="stat-card">
                <h3>soon..</h3>
                <p>Messages Today</p>
            </div>
        </div>

        <!-- Features -->
        <div class="features">
            <h1>Platform Features</h1>
            <div class="feature-grid">
                <div class="feature-card">
                    <h3>Live Collaboration</h3>
                    <p>Work together in real time with team members.</p>
                </div>
                <div class="feature-card">
                    <h3>Project Rooms</h3>
                    <p>Organize work using dedicated rooms.</p>
                </div>
                <div class="feature-card">
                    <h3>Chat & Audio</h3>
                    <p>Communicate using live chat and audio.</p>
                </div>
                <div class="feature-card">
                    <h3>Secure Access</h3>
                    <p>Authentication with session security.</p>
                </div>
            </div>
        </div>

        <!-- Recent Activity -->
        <div class="recent">
            <h1>Recent Activity</h1>
            <div class="recent-box">
                this feature will be available soon..
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div>
                <h3>Code Space</h3>
                <p>Collaborate. Build. Ship faster.</p>
            </div>
            <div>
                <h3>Quick Links</h3>
                <a href="#">Dashboard</a>
                <a href="#">Projects</a>
                <a href="#">Settings</a>
            </div>
            <div>
                <h3>Support</h3>
                <a href="#">Help Center</a>
                <a href="#">Privacy Policy</a>
                <a href="#">Terms & Conditions</a>
            </div>
        </div>
        <div class="footer-bottom">
            © 2026 Code Space. All rights reserved.
        </div>
    </footer>

</body>
</html>
