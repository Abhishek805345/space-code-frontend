<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #f4f6f8;
        }

        /* Navbar */
        nav {
            background-color: #1f2933;
            color: #fff;
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 10;
        }

        nav .logo {
            font-size: 22px;
            font-weight: bold;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 16px;
            padding: 6px 10px;
            border-radius: 4px;
        }

        nav ul li a:hover {
            background-color: #3a4a5a;
        }

        /* Hero Section */
        .hero {
            position: relative;
            height: 70vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            background-color: black;
        }

        .hero-video {
            position: absolute;
            inset: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.25; /* video visible */
        }

        .hero-content {
            position: relative;
            z-index: 1;
            text-align: center;
            color: white;
        }

        .hero-content h1 {
            font-size: 48px;
            margin-bottom: 10px;
        }

        .hero-content p {
            font-size: 20px;
        }

        /* Dashboard Content */
        .container {
            padding: 40px;
            text-align: center;
        }

        .container h1 {
            color: #1f2933;
            margin-bottom: 10px;
        }

        .container p {
            color: #555;
            font-size: 18px;
        }

        /* Button */
        .create-project-btn {
            display: inline-block;
            margin-top: 30px;
            padding: 12px 30px;
            background-color: #2563eb;
            color: white;
            text-decoration: none;
            font-size: 16px;
            border-radius: 6px;
        }

        .create-project-btn:hover {
            background-color: #1d4ed8;
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav>
        <div class="logo">Hi, <%= data.username %></div>
        <ul>
            <li><a href="/post-home/<%=data._id%>">Home</a></li>
            <li><a href="/get-room/<%=data._id%>">Project Rooms</a></li>
            <li><a href="/logout/<%= data._id %>">Logout</a></li>
        </ul>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <video autoplay muted loop playsinline class="hero-video">
            <source src="/hero.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>

        <div class="hero-content">
            <h1>Welcome, <%= data.username %> 👋</h1>
            <p>You have successfully logged in</p>
        </div>
    </section>

    <!-- Dashboard Section -->
    <div class="container">
        <h1>Dashboard</h1>
        <p>Manage your account, settings, and activities from here.</p>

        <a href="/create-project/<%= data._id %>" class="create-project-btn">
            Create Project
        </a>
    </div>

</body>
</html>
